# 🔗 Decky Links

**Launch games on SteamOS with physical media — NFC tags, floppy disks, QR codes and more.**

![Steam Deck with NFC tag reader](https://raw.githubusercontent.com/kmturley/decky-links/refs/heads/main/decky-links.jpg)

Decky Links is a [Decky Loader](https://github.com/SteamDeckHomebrew/decky-loader)
plugin for SteamOS that launches games when you present a physical object — tap a
tag, insert a disk, show a printed code. The launch URI lives *on the medium*, so
a disk paired on one Deck works on any other Deck running the plugin, as long as
the game is in that library. There is no database to keep in sync.

![Steam Deck with NFC cards, stickers and 3d-printed cartridges](https://raw.githubusercontent.com/kmturley/decky-links/refs/heads/main/decky-links-media-types.jpg)

Watch the demo: [youtube.com/watch?v=dVYhwyseCYk](https://www.youtube.com/watch?v=dVYhwyseCYk)

---

## How it works

1. **Pair.** Open a game's page in your library and press the 🔗 link icon. Hold a
   tag on the reader, or put a disk in the drive, and press **Pair** on that row.
   The game's launch address is written onto the object.
2. **Present.** Tap the tag, insert the disk, show the printed code to a camera.
   The game starts.
3. **Take it away.** Lift the tag or eject the disk and the game closes.

That is the whole loop. Nothing about the pairing is stored on the Deck, so the
object works on any Deck running the plugin that owns the game.

## Installing

There is no store release yet, so the plugin is built and installed from this
repository:

```bash
./.vscode/build.sh    # produces out/decky-links.zip
```

Then install that zip through Decky Loader — its settings carry a developer
option for installing a plugin from a file. Decky Links needs Decky's **root**
permission (declared in `plugin.json`) because mounting disks and talking to USB
readers both need it.

Full build prerequisites are under [Developer setup](#developer-setup).

## Triggers

Each trigger is a *category* of hardware, not a specific device — swap ports or
replace a dead reader and your media keeps working. All are switched on and off
individually in the plugin panel, and a switched-off trigger does no work at all.

Listed, here and in the panel, roughly by how many people will use each one.

| Trigger | Medium | Default | Status |
| --- | --- | --- | --- |
| 🏷️ NFC | Tags, stickers, cards | **on** | Mifare Classic, NTAG21x, Ultralight; PN532 (UART), ACR122U, Proxmark, nfcpy |
| 🔌 USB Storage | Thumb drives | off | Same payload format |
| 📷 Camera | Printed or on-screen QR codes | off | USB webcam; codes generated by the plugin |
| 💳 Memory Card | SD / microSD | off | Same payload format |
| 💾 Floppy | 3.5" disks in a USB drive | off | FAT-formatted, payload in `decky-links.json` |
| 💿 Optical | CD / DVD | off | Same payload format |
| 📡 IoT | MQTT messages | off | Opt-in, optional shared secret |
| 📟 Serial | Line-delimited URIs | off | Opt-in |
| 📁 File | A watched directory | off | Opt-in |

**NFC is the only trigger on out of the box**, and the one to start with: tags
cost pence, stick to anything, and need no drive. Everything else is a single
toggle away in the panel.

The rest default to off because every one of them reads media you already own
for something else — a USB stick, an SD card, the disks in your floppy drive —
and mounting those uninvited is both a surprise and a delay. A drive that is
plugged in still shows up in the panel, switched off rather than missing, so
turning it on is one press once you want it.

## Features

### Pair from the game page

The 🔗 icon on any game page opens a modal listing your enabled triggers, each
with its own **Pair** button and a live status — what is on the reader, what is
in the drive, whether it already carries a game.

With a tag on the reader *and* a disk in the drive, pressing Pair on the Floppy
row writes the disk and leaves the tag alone. Each row pairs its own medium, so
one game page can fill several objects without ever asking which one you meant.

### Auto-launch and auto-close

Present a paired medium and the game starts. Take it away and the game closes.

Only the medium that launched a game may quit it, so ejecting an unrelated disk
while you play does nothing. Both behaviours are switches in the panel if you
would rather present media and press play yourself.

### Portable payloads

What gets written is a URI — `steam://run/…`, `steam://rungameid/…` (including
non-Steam shortcuts) or `https://…` — and nothing else. A disk paired on one Deck
works on another Deck that owns the game, with no export, no sync and no
database. Every URI is checked against a strict allowlist in the backend before
it is launched, paired or turned into a QR code.

### QR codes and printable cards

The pairing modal shows a QR code for the game, which the **Camera** trigger
reads from a printed sheet or from another screen.

It will also save a two-sided 4×6" card to `~/Documents/decky-links` — Steam's
box art on the front, the QR code on the back — sized to print as a sleeve
insert, a card or a sticker.

### Formatting a blank disk

A disk with no filesystem at all is offered a **Format** button in the panel,
which takes two presses because it erases the disk. A disk that merely has no
pairing on it is never offered this — only one the plugin cannot read at all,
which is the difference between a new floppy and one holding your files.

### Sound

Presenting, pairing, failing, locking and unlocking each have their own sound, so
you know a tag landed without looking at the screen. A theme can replace all five
with its own.

### Restricted mode

Register a medium as a physical **key**: while it is present the Deck behaves
normally, and when you take it away only games handed to it on a tag or a disk
will run. See [Restricted Mode](#restricted-mode) below.

### Custom themes

Replace Steam's Home, game pages and loading screens with a theme of your own.
See [Custom Themes](#custom-themes) below.

## Restricted Mode

For handing the Deck to a child or a guest. Register a medium — a tag, a disk, a
USB stick — as the **key** from the plugin panel, and it behaves like a key left
in a lock:

| | |
| --- | --- |
| **Register a key** | Pick the trigger from the list; restricted mode is on from then on |
| **Key removed** | Locked |
| **Key present** | Unlocked |
| **Deregister** | Restricted mode off, and the key is wiped from its medium |

Nothing is stored about which object it is, and nothing is stored about whether
the Deck is locked — being locked *is* the key being absent. There is no lock
switch to get out of step with the key, and a Deck that boots with the key in a
drawer comes up locked because the key is in a drawer.

The key is a random token written onto the medium, exactly like a game pairing,
so it works on any Deck you register it with and there is no database to keep in
step. One medium cannot be both a key and a game tag.

While locked:

- **Only games you hand it will run.** A game started by presenting a medium
  plays; a game started from the library is closed with a "Restricted title"
  notice. The allowlist is the box of tags and disks you leave out — there is no
  list to build and nothing to keep in step with your library.
- **Nothing can be written or configured.** Pairing, settings, formatting and
  the Mifare key tools are refused in the backend, not merely hidden.

A game already running when the key comes out is left alone, so locking
mid-session never costs anyone their progress.

The trigger holding the key cannot be switched off while it holds it — that
would unload the key, lock the Deck, and leave no way to switch the trigger
back on. Deregister the key and the trigger is free again.

Worth knowing for an NFC key: the tag has to stay on the reader to stay
unlocked, because that is what "the key is in the lock" means. Lift it and the
Deck locks. A disk or USB stick suits an admin session better — it sits in the
drive while you work and locks the Deck when you pull it out.

### What it does not do

Steam's own menus stay reachable: the Store, Steam Settings and Desktop Mode are
not blocked, and Decky Links does not touch Steam's parental controls.

There is nothing there to use. Valve's only per-account restriction that can
bind the account holding the library is **Family View**, its older
PIN-protected mode, and Steam no longer offers to set that up on an account
that never had it. Its replacement, Steam Families, applies its controls to
*child* accounts — the opposite of what a shared Deck needs, since the games
are in the parent's library. So the rule above is entirely the plugin's own.

Losing the key means editing `settings.json` in Desktop Mode to clear
`restricted.key_hash` — there is no PIN of our own, deliberately, since a second
way in is a second thing to leave switched on by accident.

Restricted mode is a guardrail for a shared living-room device, not a security boundary.
A restricted game does visibly start before it is closed, and anyone who can read
the medium can copy it.

## Custom Themes

Pick one under **Custom Theme** in the plugin panel and Steam's interface is
replaced for as long as it is on: your theme's standby screen instead of Home, a
loading screen the moment a tag lands, its own error and locked screens, and a
screensaver after 90 seconds of nothing.

Four themes ship:

| | |
| --- | --- |
| **MS-DOS** | Text mode, block cursor, a drive that seeks |
| **Desktop 95** | Teal desktop, beveled windows, taskbar clock |
| **Arcade** | CRT bezel, coin doors, credit counter, attract mode |
| **16-Bit Console** | Copper bars, a chequerboard horizon, a boot sequence |

Steam's own menus render **above** any theme, and the theme hides itself while
one is open — so the switch that turns it back off is always reachable. Touching
a theme does nothing, rather than operating the Steam interface underneath it.

Writing your own is one HTML file dropped in `~/Documents/decky-links/themes/`,
with a folder of sounds beside it if you want them. There is no build step and no
toolchain: edit the file, reopen the panel, look at it. Copying a shipped theme
out and keeping its folder name overrides it, which is the cheapest way to tweak
one. See [docs/themes.md](docs/themes.md).

## Decky Links vs Zaparoo

[Zaparoo](https://zaparoo.org) is a cross-platform "universal loading" system
supporting Windows, Linux, SteamOS, MiSTer, Batocera and more, with a Designer
app, scripting API and a wide range of readers.

Decky Links is deliberately narrower: SteamOS only, with deeper Steam
integration. It uses Steam's native URI launch and quit behaviour, so there is no
separate service to install and no library index to maintain.

Choose Zaparoo if you need one solution across several devices, or want advanced
rules and APIs. Choose Decky Links if you want a lightweight, Steam-native
launcher on a Deck.

## Media coverage

- [Pocket Tactics — Floppy disks and the Steam Deck aren't the combo I was expecting in 2026](https://www.pockettactics.com/steam-deck/floppy-disks)
- [XDA — Someone made a system that loads their Steam games using floppy disks, and it's pretty genius](https://www.xda-developers.com/someone-made-a-system-that-loads-their-steam-games-using-floppy-disks-and-its-pretty-genius/)
- [Notebookcheck — Games going digital-only? Steam Deck user launches them with floppy disks](https://www.notebookcheck.net/Games-going-digital-only-Steam-Deck-user-launches-them-with-floppy-disks.1361271.0.html)
- [VideoCardz — Steam Deck user brings physical games back with floppy disks, sort of](https://videocardz.com/newz/steam-deck-user-brings-physical-games-back-with-floppy-disks-sort-of)
- [Club386 — This retro Steam Deck mod triggers game startup using floppy disks](https://www.club386.com/this-retro-steam-deck-mod-triggers-game-startup-using-floppy-disks/)
- [ARY News — Steam Deck user brings back floppy disks to launch digital games](https://arynews.tv/steam-deck-user-brings-back-floppy-disks-to-launch-digital-games.amp)
- [Mango Developer — Steam Deck hacker loads games from floppy disks because physical media refuses to die](https://mangodeveloper.com/articles/steam-deck-hacker-loads-games-from-floppy-disks-because-physical-media-refuses-t)
- [Lowyat.NET — Steam Deck owner brought floppy disk gaming back to life, sort of](https://www.lowyat.net/2026/400826/steam-deck-owner-brought-floppy-disk-gaming-back-to-life-sort-of/)
- [Generation Amiga — This Steam Deck mod turns floppy disks into game launchers](https://www.generationamiga.com/2026/08/07/this-steam-deck-mod-turns-floppy-disks-into-game-launchers/)
- [WindowsForum — Steam Deck Decky Links uses floppies as game launch keys](https://windowsforum.com/windows-news.4/steam-deck-decky-links-uses-floppies-as-game-launch-keys.441819/)

---

# Technical documentation

Everything below is for working on the plugin rather than using it.

## Architecture

Each trigger is a `MediaSource` running its own asyncio task at its own interval,
pushing into a shared queue that the plugin's event loop consumes. Adding a
trigger means adding a source, not touching the state machine.

```
                      ┌──────────────────────────┐
                      │      Plugin (main.py)    │
   ┌──────────────┐   │  state machine, launch,  │   ┌──────────────┐
   │ SourceManager│──►│  pairing, media registry │──►│  Frontend    │
   └──────┬───────┘   └──────────────────────────┘   │  events      │
          │                    ▲                      └──────────────┘
          │            shared asyncio.Queue
   ┌──────┴───────┬───────────┬──────────┬───────────┐
   ▼              ▼           ▼          ▼           ▼
NfcSource   StorageSource  Camera     Mqtt      FileWatch
```

Two event types flow through the queue: **`SourceEvent`** for hardware coming and
going, and **`MediaEvent`** for media being presented, removed, or still being
read. Media is tracked *per source*, so a tag and a disk can be present at once
and each has its own state in the UI.

### Repository layout

| Path | What |
| --- | --- |
| `main.py` | Plugin backend: state machine, launch/quit, pairing, RPCs |
| `sources/` | One module per trigger, plus the `MediaSource` base and `SourceManager` |
| `nfc/` | Reader backends and tag handlers (Mifare, NTAG, Ultralight …) |
| `cards/` | QR code and printable card generation |
| `decky_links/` | Settings, restricted mode, theme discovery |
| `assets/themes/` | The shipped themes, one folder each |
| `src/` | Decky UI: panel, game-page modal, background event listeners |
| `src/lib/triggerRows.ts` | The trigger row model shared by the panel and the modal |
| `scripts/` | Deck helpers, and the synthesisers that generate theme sounds |
| `tests/` | Python tests (1100+) |
| `docs/themes.md` | The theme format, for anyone writing one |
| `docs/hardware-test.md` | Ordered manual test plan for a real Deck |
| `SPEC.md` | v1 behaviour spec — state machine, launch rules, error handling |

## Developer setup

### Prerequisites

- Node.js ≥18 and `pnpm`
- Python 3 with `venv` (for the test suite; any recent version)
- Docker, for building the plugin zip
- A Steam Deck with Decky Loader, for anything hardware-related

### Install

```bash
pnpm install

python3 -m venv .venv
.venv/bin/pip install -r tests/requirements.txt pytest pytest-asyncio Pillow
```

### Build

```bash
pnpm build            # frontend only, into dist/
./.vscode/build.sh    # full plugin zip into out/
```

`build.sh` vendors Python dependencies into `py_modules/` using a
`linux/amd64` container, because the Deck is amd64 and host wheels will not load
there. It also copies `sources/`, `nfc/`, `cards/` and `assets/` into
`py_modules/` — the packaging CLI zips a fixed allowlist that does not include
them, so anything left at the top level never reaches the device.

> [!IMPORTANT]
> **`DECK_PYTHON` must match the interpreter Decky Loader runs plugins with, not
> the one SteamOS ships.** The loader is a frozen binary carrying its own Python
> (3.11.7 as of 2026-08) while SteamOS is on 3.13. Building against the wrong one
> ships extensions that fail to load, silently disabling the camera and storing
> NFC keys unencrypted. The plugin logs the answer at startup:
>
> ```bash
> pnpm logs | grep -A4 "Python runtime"
> ```
>
> Re-check after any Decky Loader update. The build guards both version-tagged
> and abi3 extensions against `DECK_PYTHON` and fails loudly on a mismatch.

### Deploy and debug

```bash
pnpm deploy           # rsync the tree to the Deck
pnpm deck:restart     # restart plugin_loader — required, see below
pnpm deck:status      # devices, mounts, plugin process, dependency imports
pnpm logs             # tail the plugin log
pnpm logs:follow      # …live
pnpm deck:udev        # watch block-device events live
```

A deploy alone is not enough: the plugin's uid is fixed when its process spawns,
so mounting disks needs a loader restart to take effect. `deck:status` reports
`STALE` when the deployed files are newer than the running process.

### Testing

```bash
pnpm test                              # the whole suite
.venv/bin/pytest -q tests/test_plugin.py
```

Three dependency-import tests fail on a non-Linux dev machine (`pyudev`,
`paho-mqtt`, `zxingcpp`); they pass on the Deck. Everything else runs anywhere.

For hardware, work through [`docs/hardware-test.md`](docs/hardware-test.md) — ten
ordered steps with commands and pass criteria. The order matters: a stale deploy
or a leaked mount makes every later result meaningless.

## Security

- URIs are validated backend-side against a strict allowlist: `steam://run/*`,
  `steam://rungameid/*` and `https://` only. The same check gates launching,
  pairing and QR generation.
- Settings are validated before being persisted, and again when loaded from disk.
- Virtual triggers (MQTT, serial, file watcher) are **off by default** and
  opt-in; MQTT supports a shared secret.
- Disks are mounted read-only, `nosuid,nodev,noexec`, with the filesystem type
  probed by `blkid` and checked against a small allowlist rather than left for
  the kernel to guess. Pairing remounts read-write for the one write and puts
  it back, repeating the hardening flags — a remount does not inherit them.
- The only thing ever written to a medium is `decky-links.json` at its root,
  and it is written straight to the medium's own mount point; nothing is staged
  elsewhere on the Deck first. The write is refused unless `/proc/mounts` still
  shows that path as a mount of that device, so a mount that disappeared cannot
  turn the write into a stray file on the Deck's filesystem.
- Mifare Classic writes skip trailer blocks, so keys and access bits cannot be
  corrupted.
- Writes are refused before the first byte when a tag is too small for the
  payload, so a failed pair leaves the previous value intact.
- Themes are data, not code: a theme's markup is rendered inert, its id and
  filenames are pattern-checked before becoming paths, and asset reads are
  confined to the theme's own folder.

## Troubleshooting

| Symptom | Check |
| --- | --- |
| Panel shows nothing / stale state | `pnpm logs` — the plugin emits status on change and on a 2s tick |
| `must be superuser to use mount` | The loader did not restart; `pnpm deck:status` shows the process user |
| Camera never connects | `pnpm deck:status` import check, and the `Python runtime` note above |
| Reader not detected | Auto-detection only matches known USB-serial vendor IDs; set the device path in settings |
| Drive letter drifts `sda`→`sdb` | A leaked mount is pinning the node — `grep decky-links /proc/mounts` |
| A theme does not appear in the picker | The folder name is not a valid id, or it has no `theme.html` |

## License

GNU General Public License v3.0 — see [LICENSE](./LICENSE).
