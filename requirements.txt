cryptography
pyserial                       # serial port access for PN532 UART reader
adafruit-circuitpython-pn532   # PN532 NFC chip driver (requires pyserial)
ndeflib                        # NDEF record encoding/decoding
nfcpy                          # optional nfcpy reader backend
pyudev                         # Linux-only; StorageSource degrades gracefully when not available
pyzbar                         # QR decoding; CameraSource degrades gracefully when not available
Pillow                         # image handling for pyzbar input frames
paho-mqtt                      # MqttSource; degrades gracefully when not available
